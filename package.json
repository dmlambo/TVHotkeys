{
  "scripts": {
    "build": "tsc && cp public/* dist/",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "web-ext-types": "^3.2.1"
  },
  "dependencies": {
    "@types/firefox-webext-browser": "^143.0.0"
  }
}
