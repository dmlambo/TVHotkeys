{
  "scripts": {
    "build": "tsc && cp public/manifest.json dist/manifest.json && cp public/options.html dist/options.html",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "web-ext-types": "^3.2.1"
  }
}
